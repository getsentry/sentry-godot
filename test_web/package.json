{
  "name": "sentry-godot-web-tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "serve": "tsx serve.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "tsx": "^4.0.0"
  }
}
